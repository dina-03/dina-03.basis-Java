package lesson40;

public class Main {
    public static void main(String[] args) {
        double roomWidth = 5.7; // метры
        double roomLength = 3.3; // метры
        double tilesPrice = 3.0;
        TilesApplication.start(roomWidth, roomLength, tilesPrice);
    }
}
