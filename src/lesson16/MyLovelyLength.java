package lesson16;

public class MyLovelyLength {
    public static void main(String[] args) {
        String name = "Василий";
        int length = name.length();
        System.out.println(length);
        char c = name.charAt(name.length()-1);
        System.out.println(c);
    }
}
